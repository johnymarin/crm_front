
import 'package:flutter/material.dart';

class CityModify extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create City')
      ),
      body: Container(),
    );
  }
}
